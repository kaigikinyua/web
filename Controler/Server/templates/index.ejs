<!DOCTYPE html>
<html>
<head>
	<title>| share ! server|</title>
	<link rel="stylesheet" type="text/css" href="static/main.css">
	<script type="text/javascript" src='static/vue.js'></script>
</head>
<body>
	<div id="topBarner" class="topBarner">
		<p class="outcast">S</p>hare
		<div class="items">
			<ul class="horizontalList">
				<li v-bind:class='{active:videos}' v-on:click="videos = true; documents = false; image = false">Videos</li>
				<li v-bind:class='{active:documents}' v-on:click="videos = false; documents = true; image = false">Documents</li>
				<li v-bind:class='{active:image}' v-on:click="videos = false; documents = false; image = true">Pictures</li>
			</ul>
		</div>
	</div>
	<div style="height:150px;"></div>
	<div v-show="video()" class="content" id="video">
		<% for(var i=0;i < videos.length;i++){%>
			<div class="module">
				<video src="/video/<%=videos[i].name%>"controls></video>
				<div class="videoDetails">
					<%=videos[i].name %>	
				</div>
				<a href="/video/<%=videos[i].name%>" download>
					<button>Download</button>
				</a>
			</div>
		<% } %>
	</div>
	<div v-show="documents()" id="documents">
		<% for(var i=0;i < documents.length;i++){%>
				<%=documents[i].name %>
				<a href="/documents/view/<%=documents[i].name%>">
					<button>FullScrean</button>
				</a>
				<embed src="documents/view/<%=documents[i].name%>" height="100%" width="50%"/>
		<% } %>
	</div>
	<div v-show="image()" class="content" id="pictures">
		<% for(var i=0;i < pictures.length;i++){%>
			<div class="module">
				<img src="/pictures/<%=pictures[i].name%>"/>
					<%=pictures[i].name %>	
			</div>
		<% } %>
	</div>
	<script src='static/dynamic.js'></script>
</body>
<script type="text/javascript" src="static/myvue.js" height="200px;" width="200px;"></script>
</html>